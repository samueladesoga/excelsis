   <% @private_services = data["contentful"]["service"].values.find_all{|serv|serv["sector"] == "private"}.group_by{|v|v["type"]} %>
    <section class="slice sct-color-1">
        <div class="container">
            <div class="section-title-wrapper style-1 v1 no-margin">
                <h3 class="section-title center">
                    <span>HR Learning & Development</span>
                </h3>
            </div>

            <p class="text-center mb-30">
            HR learning and develpment courses are packaged to cover specific areas of Human Resources and our prospective student can choose one of our pre-packaged courses or we can design a bespoke programme suited to your needs.
            </p>
        </div>
    </section>

    <% @private_services.each_with_index do |(type, service_data), index| %>
    <% if index.even? %>
        <section class="slice sct-color-1">
    <% else %>
        <section class="slice sct-color-2">
    <% end %>    
        <div class="container">
            <div class="row">
                <% if index.even? %>
                    <div class="col-md-6">
                        <img src="images/prv/block-img-1.jpg" class="img-responsive">                 
                    </div>
                <% end %>  
                <div class="col-md-6">
                    <div class="section-title-wrapper style-1 v1">
                        <h3 class="section-title section-title-sm left">
                            <span><%= type %></span>
                        </h3>
                    </div>
                    <% service_data.each do |sd| %>
                    <ul>
                        <li>
                            <%= sd["name"] %>
                            <% if not sd["description"].nil? %>
                                <ul>
                                    <li>
                                        <%= sd["description"] %>
                                    </li>
                                </ul>
                            <%end%>
                        </li>
                    </ul>
                    <% end %>
                    <a href="contact-1.html" class="btn btn-b-base-1 mt-20">Find out more</a>
                </div>
                <% if not index.even? %>
                    <div class="col-md-6">
                        <img src="images/prv/block-img-2.jpg" class="img-responsive">                            
                    </div>
                <% end %>  
            </div>
        </div>
    </section>
    <% end %>